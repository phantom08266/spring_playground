spring:
  cloud:
    openfeign:
      client:
        config:
          default:
            connectTimeout: 1000
            readTimeout: 3000
            loggerLevel: BASIC
          demo-client: # DemoFeignClient?? ??? Client ?? ?
            connectTimeout: 1000
            readTimeout: 10000
            loggerLevel: HEADERS # FeignCustomLogger? Logger.Level? ???. (https://docs.spring.io/spring-cloud-openfeign/reference/spring-cloud-openfeign.html)
            # Logger.Level
            # NONE  = No logging.
            # BASIC = Log only the request method and URL and the response status code and execution time.
            # HEADERS = Log the basic information along with request and response headers.
            # FULL = Log the headers, body, and metadata for both requests and responses.


            # ****??? ??? ??? ?? ??!!! *****
            # ??? ??? ??? ????? ? ?? SynchronousMethodHandler????? ???? ?? executeAndDecode? ????.
            # ?? loggerLevel??? None? ????? ?? ??? ??? ??? ??? ?? ?? ??? ???? ??.
feign:
  url:
    prefix: http://localhost:8080/server